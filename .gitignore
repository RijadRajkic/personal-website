# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local-only design bundle reference imports (Claude Design v1+v2 prototypes & chats)
docs/design-bundles/

# NOTE: on the `feat/moving` transfer branch, .claude/, scripts/, and docs/code-bmo-*.md
# are intentionally un-ignored so they can be carried to a new machine. They stay
# gitignored on every other branch — do not merge this branch.
